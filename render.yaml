# Set all env vars in Render Dashboard → Environment (DB_*, N8N_*, etc.)
services:
  - type: web
    name: n8n
    env: node
    plan: free
    autoDeploy: false
    buildCommand: npm ci
    startCommand: ./start.sh
    healthCheckPath: /healthz
